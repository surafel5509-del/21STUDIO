# Space Shooter sample plan

The `app` module is the runnable sample host. Add `Transform`, `Sprite`, `Rigidbody`, and `Collider` components to a player entity, then register movement, collision, and render systems. This keeps gameplay code separate from reusable engine modules.
